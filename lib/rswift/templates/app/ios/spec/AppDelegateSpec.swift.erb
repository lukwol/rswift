import Quick
import Nimble

@testable import <%= @name %>

class AppDelegateSpec: QuickSpec {

    override func spec() {

        var sut: AppDelegate!

        beforeEach {
            sut = AppDelegate()
        }

        afterEach {
            sut = nil
        }

        it("should pass") {
            expect(sut).toNot(beNil())
        }
    }
}
